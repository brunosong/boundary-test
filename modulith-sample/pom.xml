<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.brunosong.sample</groupId>
        <artifactId>boundary-test</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>modulith-sample</artifactId>
    <name>modulith-sample</name>
    <description>Spring Modulith 로 모듈 경계를 강제하는 예제</description>

    <!--
      ArchUnit 쪽과 모듈 구성이 다르다.

      ArchUnit 은 규칙(archunit-rules)과 검사 대상(target-service)을 모듈로 나눌 수 있었다.
      규칙이 대상과 독립이라 다른 저장소에 복사할 수도 있다.

      Spring Modulith 는 나눌 수가 없다. 모듈 경계를 앱의 패키지 구조에서 읽어내고,
      검증도 그 앱의 test 소스에서 자기 자신을 대상으로 돌린다. 그래서 여기는 모듈 하나다.
      이것 자체가 두 방식의 차이라서 억지로 맞추지 않았다.
    -->
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <classifier>boot</classifier>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
